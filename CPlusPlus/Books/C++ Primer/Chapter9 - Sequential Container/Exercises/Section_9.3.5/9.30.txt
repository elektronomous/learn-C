What, if any, restrictions does using the version of resize
that takes a single argument place on the element type?

(answer):
= When you resize the class type, the class must be has default-constructor.