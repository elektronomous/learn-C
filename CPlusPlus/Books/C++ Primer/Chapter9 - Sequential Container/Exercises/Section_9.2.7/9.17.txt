Assuming c1 and c2 are containers, what (if any) constraints does
the following usage place on the types of c1 and c2?

    if (c1 < c2)

(answer):
= If the container has a type that's a class type, then the above code
  will be valid if the class type support relational operator.