#include <iostream>
#include <string>

using std::cout,
      std::cin,
      std::endl,
      std::string;

int main(void) {
    string ch;
    size_t
    while (cin >> ch)
        cout << "The character: " << ch << endl;
    
    return 0;
}