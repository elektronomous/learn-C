#include <iostream>
#include <memory>
#include "StrBlob.hpp"

using namespace std;

int main(void) {
    /* 12.3 
    
    const shared_ptr<int> p = make_shared<int>(20);
    shared_ptr<int> q = make_shared<int>(30);
    p = q; 
    */
}