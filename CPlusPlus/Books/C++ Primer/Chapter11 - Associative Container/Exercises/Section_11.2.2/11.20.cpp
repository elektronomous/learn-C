#include <iostream>
#include <iterator>
#include <list>
#include <vector>
#include <map>

using namespace std;

int main(void) {
    map<vector<int>::iterator, int> vecToInt;
    map<list<int>::iterator, int> listToInt;    

    return 0;
}

// because the key has