#include <iostream>
#include <vector>

using namespace std;

int main(void) {
    vector<string> excludedWords {"The", "But", "And", "An", "A", "the", "but", "and", "an", "a"};
    /* the advantages using set is the value is sorted and when you find sorted value, it's faster
       then finding a value in a random element.*/
}