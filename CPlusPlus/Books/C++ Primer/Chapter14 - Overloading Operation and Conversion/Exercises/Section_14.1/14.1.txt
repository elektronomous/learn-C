In what ways does an overloaded operator differ from built-in
operator? In what ways are overloaded operators the same as the
built-in operators?

(answer):
= They're differ when in their implementations. They're the same in
  their application.