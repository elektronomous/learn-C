Add dereference and arrow operators to your StrBlobPtr class and
to the ConstStrBlobPtr class that you defined in the exercise 12.22
from $ 12.1.6 (p. 476). Note that the operators in ConstStrBlobPtr
must return const references because the data member in ConstStrBlobPtr
points to a const vector.