Explain the difference between prefix and postfix increment.
= The prefix return the incremented(or decremented) object as lvalue, while
  the postfix return the copy original of the object as rvalue, and incremented in
  subsequent expression.
