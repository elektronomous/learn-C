{
  "tasks": [
    {
      "type": "shell",
      "label": "C/C++: gcc BUILD ACTIVE FILE",
      // "command": "g++.exe",
      "command": "gcc",
      "args": [
        "-fdiagnostics-color=always",
        "-g",
        "-Wall",
        "-Werror",
        // "-save-temps", => if you wanna extract the completed result of compiler
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": "build",
      "detail": "compiler: /usr/bin/gcc"
    }
  ],
  "version": "2.0.0"
}
